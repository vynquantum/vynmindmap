---
name: vynmm-mindmap
description: >-
  Create, edit, or combine VynMM mind maps (.vmm files). Use when the user wants
  to make a mind map, brainstorm a topic into a map, outline something visually,
  convert notes into a .vmm, or merge several maps into one. Authors a Markdown
  outline and converts it with the vynmm CLI.
---

# Creating VynMM mind maps

The instructions live in the repo so every agent and editor reads the same copy.

**Read [skills/create-vmm/SKILL.md](../../../skills/create-vmm/SKILL.md) and
follow it.** It covers the Markdown format, the frontmatter and map settings,
per-topic metadata, multi-tab files, merging maps, and the CLI/MCP commands.
